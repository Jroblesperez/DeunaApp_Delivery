import {NextResponse} from 'next/server';import {initiatives} from '@/lib/demo-data';import {envelope,validateId} from '@/lib/api';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!validateId(id))return NextResponse.json({data:null,status:'ERROR',source:'FlowOS',lastUpdated:new Date().toISOString(),warnings:['Invalid initiative ID'],coverage:0},{status:400});const item=initiatives.find(x=>x.id===id);if(!item)return NextResponse.json(envelope(null,0,['Initiative not found']),{status:404});return NextResponse.json(envelope(item))}
