import {NextResponse} from 'next/server';import {envelope} from '@/lib/api';import {organizationContext,syncRuns} from '@/lib/enterprise/data';export async function GET(){return NextResponse.json(envelope({health:organizationContext.syncHealth,lastSuccessful:syncRuns.find(r=>r.status==='COMPLETED'),nextSync:'2026-07-31T09:55:00Z',coverage:96,staleSources:['Jira Service Management']},96,['Una fuente presenta datos desactualizados']))}
