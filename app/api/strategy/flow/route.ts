import {NextResponse} from 'next/server';import {envelope} from '@/lib/api';import {portfolioFlow} from '@/lib/strategy/data';export async function GET(){return NextResponse.json(envelope(portfolioFlow(),96))}
