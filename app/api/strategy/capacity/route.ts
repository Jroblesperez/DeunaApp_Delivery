import {NextResponse} from 'next/server';import {envelope} from '@/lib/api';import {strategyCapacity} from '@/lib/strategy/data';export async function GET(){return NextResponse.json(envelope(strategyCapacity(),96))}
