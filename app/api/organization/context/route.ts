import {NextResponse} from 'next/server';import {envelope} from '@/lib/api';import {organizationContext} from '@/lib/enterprise/data';export async function GET(){return NextResponse.json(envelope(organizationContext,organizationContext.coverage))}
