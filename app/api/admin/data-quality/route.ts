import {NextResponse} from 'next/server';import {envelope} from '@/lib/api';export async function GET(){return NextResponse.json(envelope({health:84,coverage:96,critical:4,warnings:6,insufficientData:1,signals:[{key:'unmapped-status',value:2,status:'CRITICAL'},{key:'missing-team',value:4,status:'CRITICAL'},{key:'duplicates',value:null,status:'INSUFFICIENT_DATA'}]},96,['Una fuente desactualizada']))}
