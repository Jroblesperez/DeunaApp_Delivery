import {NextResponse} from 'next/server';import {envelope} from '@/lib/api';import {initiatives,ecos} from '@/lib/demo-data';import {actions,insights} from '@/lib/metrics';export async function GET(){return NextResponse.json(envelope({health:72,confidence:68,atRisk:initiatives.filter(x=>['HIGH','CRITICAL'].includes(x.risk)).length,criticalDecisions:actions().filter(x=>x.priority==='P0').length,ecos,brief:insights()}))}
