import {NextResponse} from 'next/server';import {envelope} from '@/lib/api';import {integrations} from '@/lib/enterprise/data';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const item=integrations.find(i=>i.id===id);return item?NextResponse.json(envelope(item,item.coverage??0)):NextResponse.json(envelope(null,0,['Integración no encontrada']),{status:404})}
