import {AdminShell,IntegrationCard} from '@/components/enterprise/platform';import {JiraIntegrationPanel} from '@/components/jira-live';import {PageHeader} from '@/components/ui';import {integrations} from '@/lib/enterprise/data';
export default function Page(){return <AdminShell><PageHeader eyebrow="INTEGRACIONES" title="Integration Hub" description="Conexión Jira LIVE read-only y fuentes DEMO claramente separadas." action={<span className="pill">Jira · READ_ONLY</span>}/><JiraIntegrationPanel/><div className="section-title"><div><h2>Fuentes DEMO y planificadas</h2><p>Estas tarjetas conservan el fallback existente y no se mezclan con el snapshot LIVE.</p></div></div><div className="integration-grid">{integrations.map(item=><IntegrationCard item={item} key={item.id}/>)}</div></AdminShell>}
