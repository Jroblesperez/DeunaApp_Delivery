'use client';import {useState} from 'react';import {AdminShell} from '@/components/enterprise/platform';import {PageHeader} from '@/components/ui';import {projects as initial} from '@/lib/enterprise/data';
export default function Page(){const [projects,setProjects]=useState(initial);return <AdminShell><PageHeader eyebrow="JIRA · CONFIGURACIÓN" title="Mapeo de proyectos" description="Selecciona el alcance, asigna ECO, tipo y prioridad; no modifica proyectos en Jira." action={<span className="pill">{projects.filter(p=>p.included).length} de {projects.length} incluidos</span>}/><div className="project-grid">{projects.map(project=><article className={`project-card card ${project.included?'selected':''}`} key={project.id}><div className="between"><span className="project-key">{project.key}</span><label className="switch"><input type="checkbox" checked={project.included} onChange={()=>setProjects(projects.map(p=>p.id===project.id?{...p,included:!p.included}:p))}/><i/></label></div><h3>{project.name}</h3><div className="project-meta"><span>{project.type}</span><span>{project.eco}</span></div><div className="mix-row"><span>Cobertura</span><div className="progress"><i style={{width:`${project.coverage}%`}}/></div><b>{project.coverage}%</b></div><small>Última sync: {project.lastSync}</small></article>)}</div></AdminShell>}
