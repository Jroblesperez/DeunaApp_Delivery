import {PageHeader,Progress,HealthBadge} from '@/components/ui';import {initiatives} from '@/lib/demo-data';import {portfolioMix} from '@/lib/metrics';export default function Page(){const okrs=['Crecimiento rentable','Confianza y seguridad','Experiencia simple','Escala operacional','Plataforma resiliente'];return <><PageHeader eyebrow="STRATEGIC PORTFOLIO" title="Portfolio & OKR Alignment" description="Balance de inversión y trazabilidad entre resultados e iniciativas."/><div className="dashboard-grid"><section className="card panel"><h3>Portfolio Mix</h3>{portfolioMix().map(x=><div className="mix-row" key={x.type}><span>{x.type.replaceAll('_',' ')}</span><Progress value={Math.round(x.value/9)}/><b>{x.value} SP</b></div>)}</section><section id="okr" className="card panel"><h3>OKR Alignment</h3>{okrs.map((o,i)=><div className="decision" key={o}><HealthBadge status={i===1?'CRITICAL':i===3?'WARNING':'HEALTHY'}/><div><h4>O{i+1} · {o}</h4><p>{initiatives.filter(x=>x.okr?.startsWith(`O${i+1}`)).length} iniciativas alineadas · progreso {62+i*4}%</p></div><b>{62+i*4}%</b></div>)}</section></div></>}
