'use client';import {useMemo,useState} from 'react';import Link from 'next/link';import {initiatives,ecoNames} from '@/lib/demo-data';import {HealthBadge,Progress,Pill} from './ui';
export function InitiativeTable(){const [q,setQ]=useState('');const [eco,setEco]=useState('');const [status,setStatus]=useState('');const filtered=useMemo(()=>initiatives.filter(x=>(x.name.toLowerCase().includes(q.toLowerCase())||x.id.toLowerCase().includes(q.toLowerCase()))&&(!eco||x.eco===eco)&&(!status||x.status===status)),[q,eco,status]);return <><div className="quick">{['Todas','En riesgo','DEV terminado sin producción','En matriz de riesgo','Con planes de acción','Vencidas','Sin equipo','Próximas a release'].map(x=><button key={x}>{x}</button>)}</div><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar por ID, iniciativa u owner..."/><select value={eco} onChange={e=>setEco(e.target.value)}><option value="">Todos los ECOs</option>{ecoNames.map(x=><option key={x}>{x}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Todos los estados</option>{[...new Set(initiatives.map(x=>x.status))].map(x=><option key={x}>{x}</option>)}</select><Pill>{filtered.length} resultados</Pill></div><div className="table-wrap"><table><thead><tr><th>INICIATIVA</th><th>ECO / EQUIPO</th><th>ESTADO</th><th>PROGRESO</th><th>RIESGO</th><th>CONFIANZA</th><th>FECHA OBJETIVO</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td><Link href={`/initiatives/${x.id}`}><strong>{x.name}</strong><span className="muted">{x.id} · {x.type.replaceAll('_',' ')}</span></Link></td><td><strong>{x.eco}</strong><span className="muted">{x.team??'Sin equipo'}</span></td><td><span className="pill">{x.status.replaceAll('_',' ')}</span><div className="muted">{x.daysInStatus} días aquí</div></td><td style={{minWidth:120}}><Progress value={x.progress}/><span className="muted">{x.progress}%</span></td><td><HealthBadge status={x.risk}/></td><td><strong>{x.confidence}%</strong></td><td>{x.target}</td></tr>)}</tbody></table></div></>}
