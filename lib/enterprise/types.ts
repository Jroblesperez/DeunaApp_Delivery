import type {Role} from '../types';
export interface OrganizationContext {organizationId:string;organizationName:string;businessUnit:string;country:string;timezone:string;fiscalYear:number;activeQuarter:string;quarterStart:string;quarterEnd:string;quarterDay:number;quarterDays:number;dataMode:'DEMO'|'LIVE';lastSync:string;syncHealth:'HEALTHY'|'DEGRADED'|'ERROR';health:number;coverage:number;connectedSources:string[]}
export type IntegrationStatus='CONNECTED'|'DEGRADED'|'SYNCING'|'READY_TO_CONNECT'|'NOT_CONFIGURED'|'ERROR'|'DISABLED';
export interface Integration {id:string;name:string;category:string;status:IntegrationStatus;mode:'READ_ONLY'|'CONFIGURATION_ONLY'|'PLANNED';lastSync?:string;coverage?:number;errors:number;objects:number;permissions:string[];frequency:string;owner:string;action:string;demo:boolean}
export interface EnterpriseSession {id:string;userId:string;name:string;role:Role;organizationId:string;scope:string;provider:'DEMO'|'ENTRA'|'ATLASSIAN';expiresAt:string;demo:boolean}
export interface SyncRun {id:string;organizationId:string;mode:'FULL'|'INCREMENTAL'|'MANUAL'|'SCHEDULED';status:'PENDING'|'RUNNING'|'PARTIAL'|'COMPLETED'|'FAILED'|'CANCELLED';startedAt:string;completedAt?:string;checkpoint?:string;processed:number;changed:number;errors:number;coverage:number;sources:string[];durationSeconds?:number;correlationId:string}
