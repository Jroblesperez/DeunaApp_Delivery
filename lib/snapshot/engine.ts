import {syncRuns} from '../enterprise/data';import type {SyncRun} from '../enterprise/types';
export interface SnapshotStore<T>{latestValid(organizationId:string,source:string):Promise<T|null>;save(organizationId:string,source:string,data:T,checkpoint:string):Promise<void>}
export interface SyncScheduler{schedule(organizationId:string,expression:string):Promise<void>}
export class DemoSnapshotEngine {private runs=[...syncRuns];private checkpoints=new Map<string,string>([['org-deuna-ecuador:jira','2026-07-31T09:40:02Z']]);start(organizationId:string,mode:SyncRun['mode']='MANUAL'){const run:SyncRun={id:`SYNC-DEMO-${this.runs.length+1}`,organizationId,mode,status:'COMPLETED',startedAt:new Date('2026-07-31T09:45:00Z').toISOString(),completedAt:new Date('2026-07-31T09:45:22Z').toISOString(),checkpoint:'2026-07-31T09:45:22Z',processed:2521,changed:37,errors:0,coverage:96,sources:['Jira Software','Jira Product Discovery','Jira Risk Project','Jira Releases'],durationSeconds:22,correlationId:`corr-demo-${this.runs.length+1}`};this.runs.unshift(run);this.checkpoints.set(`${organizationId}:jira`,run.checkpoint!);return run}history(organizationId:string){return this.runs.filter(r=>r.organizationId===organizationId)}checkpoint(organizationId:string,source='jira'){return this.checkpoints.get(`${organizationId}:${source}`)??null}deduplicate<T extends {id:string}>(items:T[]){return [...new Map(items.map(item=>[item.id,item])).values()]}resolveSnapshot<T>(fresh:T|null,lastValid:T|null){return fresh??lastValid}}
export const snapshotEngine=new DemoSnapshotEngine();
